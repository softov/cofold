---
title: the overlay and the commands
status: done
depends: [task-01-usage-turns-export-listskills-shipped.md]
layer: papo
---

## Objective

the overlay and the commands.

## Files

- (see the plan)

## Steps

(as the plan says)

## Validation

- screen tests: `/status` shows the model and the session id; `/cost` shows the token totals; `/help` lists `Quit ctrl+q`; `/skills` picker inserts `/review `; `/export` writes the file and says where; `/retry` sends the last input again; `/theme` changes the theme; `/clear` opens a new conversation; `/config` shows redacted keys.

## Resume

Done; see the plan's Resume state.
