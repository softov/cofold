---
title: the service
status: done
depends: [task-01-sdk-boundary-projection.md]
layer: papo
---

## Objective

the service.

## Files

- (see the plan)

## Steps

- `createClaudeChat`: warm query, `say` / `wait` / `subscribe`, `sessions` / `snapshot` / `remove`, `models` / `skills`, settings mapping, `cancel`, `compact`.
- `permissions.ts`: `approve` / `deny` / `answer` over the pending promise; `always`.

## Validation

- over `fakeQuery`: the seven scenarios of decision 10; a second `say` while running is `writer_busy`.

## Resume

Done; see the plan's Resume state.
