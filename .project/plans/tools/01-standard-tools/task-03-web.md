---
title: web
status: done
depends: [task-02-shell.md]
layer: tools
---

## Objective

web.

## Files

- `CREATE: src/types/web.ts`, `src/web.ts`, `src/search/{brave,tavily,duckduckgo}.ts`, `src/web.test.ts` (injected `fetch`).

## Steps

(as the plan says)

## Validation

- HTML reduced to text; size cap; `web_search` absent with no provider, present with one; provider failover; each provider's response parsed from a fixture.

## Resume

Done; see the plan's Resume state.
