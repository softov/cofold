---
title: shell
status: done
depends: [task-01-paths-files.md]
layer: tools
---

## Objective

shell.

## Files

- `CREATE: src/types/shell.ts`, `src/shell.ts`, `src/shell.test.ts`.

## Steps

(as the plan says)

## Validation

- `echo` on both shells; exit code carried; timeout kills and reports `timedOut`; output capped with the marker; the run's abort signal kills the child.

## Resume

Done; see the plan's Resume state.
