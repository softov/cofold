{
  "name": "facio-examples-commands",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@facio/commands": "workspace:*",
    "@facio/docs": "workspace:*",
    "@facio/mcp": "workspace:*",
    "@facio/remote": "workspace:*",
    "@facio/terminal": "workspace:*"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.3.6"
  }
}
