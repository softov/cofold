{
  "name": "cofold-examples-commands",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cofold/commands": "workspace:*",
    "@cofold/mcp": "workspace:*",
    "@cofold/remote": "workspace:*",
    "@cofold/terminal": "workspace:*"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.3.6"
  }
}
